import { defineConfig } from 'vite';

export default defineConfig({
  // Base path must match the GitHub repository name for assets to resolve correctly on GitHub Pages
  base: '/Ellieslogistics/'
});
